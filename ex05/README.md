ex05: 多档位触摸调速呼吸灯

接线建议：
- 触摸模块数字输出接到数字引脚 2。
- LED（限流电阻）连接到 PWM 引脚 9。

功能：
- LED 持续呼吸（平滑增减亮度）。
- 每次触摸一次，档位在 1->2->3->1 循环，改变呼吸速度（慢/中/快）。

可调参数：
- 如果触摸模块为低电平有效，请修改 `TOUCH_ACTIVE` 为 `LOW`。
- 可通过更改 `interval` 值调整各档位的节奏。